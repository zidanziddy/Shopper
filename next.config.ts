/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.ibb.co", "avatars.githubusercontent.com"], // Add GitHub's avatar domain
  },
};

module.exports = nextConfig;
